---
title: "CFB-rmarkdown"
output: html_document
---

```{r}
library(shiny)
library(readr)
library(rvest)
library(tidyverse)
library(data.table)
library(DT)
recData<-read_csv("data/2020.csv") %>%
  filter(stars ==4 | stars==5) %>%
        filter(stateProvince=="NC")

names(which.max(table(recData$committedTo)))

```