---
title: "CFB-rmarkdown"
output: html_document
---

```{r}
library(shiny)
library(readr)
library(rvest)
library(tidyverse)
library(data.table)
library(DT)
recData<-read_csv("data/allYears.csv")
tempor<-recData[order(recData$stateProvince),]

names(which.max(table(recData$position)))
unique(tempor$position)

```